A java application which takes in player letters through the command prompt
and finds the highest-scoring words they can make using the dictionary.txt file

Note: you must include the location where you have stored the dictionary.txt file to
	use this application